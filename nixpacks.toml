[phases.setup]
nixPkgs = ["php82"]

[phases.install]
cmds = ["test -f composer.json && composer install --no-dev --no-interaction || echo 'No composer.json or dependencies needed'"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "php -S 0.0.0.0:$PORT"

